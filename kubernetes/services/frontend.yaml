kind: Service
apiVersion: v1
metadata:
  name: foofoo
  namespace: idiot-or-not
spec:
  type: NodePort
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      nodePort: 31000
  selector:
    app: foofoo