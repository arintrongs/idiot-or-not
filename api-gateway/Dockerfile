FROM golang:1.13.4-alpine3.10

WORKDIR /go/src/app
COPY . .

ARG LEADERBOARD_URL
ARG QUESTION_URL
ARG SUBMIT_URL

ENV LEADERBOARD_URL=$LEADERBOARD_URL
ENV QUESTION_URL=$QUESTION_URL
ENV SUBMIT_URL=$SUBMIT_URL

RUN go get -d -v ./...
RUN go install -v ./...

CMD ["app"]